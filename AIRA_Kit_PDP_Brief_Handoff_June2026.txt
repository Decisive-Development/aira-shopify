================================================================
AIRA — KIT PDP FIX: IMPLEMENTATION HANDOFF
Brief: Brief.txt (Kit PDP fix, 2026-06-11) | Implemented: 2026-06-11
Verified: rendered-page assertions (local dev server), 390px
headless screenshot, 4-agent adversarial audit vs Brief.txt.
================================================================

STATUS: implemented and verified. theme-editor-sync was live the
whole session; every edit was re-verified after sync round-trips
(one revert was caught and re-applied).

DONE (acceptance checklist)
---------------------------
[x] Handle rename CONFIRMED already done by Tom — both
    /products/aira-kit and /products/aira-starter-kit resolve to
    the "Aira Kit" product on the dev preview. Theme link sweep
    complete: index.json SLEEP + TRAIN section CTAs were the last
    two /products/aira-starter-kit links → now /products/aira-kit.
    Zero theme links target the old handle.
    OPEN: (a) live-store 301 not observable from outside yet
    (store password-gated; everything 302s to /password) —
    re-verify old URL → new URL after the password drops.
    (b) The ADMIN NAVIGATION "Shop" menu item historically points
    at /products/aira-starter-kit (claudeinfo.txt §nav). Menus
    live in admin (Content → Menus), not the theme — the auto
    redirect masks it, but Tom should repoint it to
    /products/aira-kit.
[x] "nose bridge" placement copy: zero theme-wide. Fixed in
    product.json (dup section removed), index.json HOW IT WORKS
    step 2, product.refill.json how-to accordion AND its lower
    how-to-use section step 2, plus the how-to-use.liquid schema
    default (new instances are born canonical).
    NOTE for sign-off: a literal "nose bridge" grep still hits
    2 places — "For narrower nose bridges" (page.how-it-works.json
    + how-it-works-step.liquid default). That is band-SIZE copy,
    not placement copy; rewriting it would corrupt the sizing
    guidance. Tom to accept this exception explicitly (the brief's
    "frozen homepage FAQ" exception is moot — the homepage FAQ
    contains no "nose bridge" string at all).
[x] One-time Purchase is default-selected (verified in rendered
    DOM: one-time card carries is-selected, subscription does
    not; selling_plan input starts empty).
[x] Subscription card: "Kit + Monthly Refills" + chip "Best
    value" + the four new bullets, entered verbatim (em/en-dashes
    byte-verified by audit).
[x] Exactly ONE how-to on the page — buy-box accordion, 3
    canonical steps + leaflet note as a trailing paragraph. Lower
    "3 steps to better breathing" section removed (JSON move).
[x] Before/After is the first section under the buy box
    (verified in rendered DOM order).
[x] Claims: "fixes", "Significantly reduces", "very first
    night", "[LIST OF COUNTRIES]" — zero hits on product.json
    (grep + rendered HTML).
[x] Page FAQ: the three mandated mechanical fixes applied
    verbatim — see COHABITATION below for three additional FAQ
    edits that belong to the earlier compliance sweep, not this
    brief.
[x] Gallery: placeholder-slot markup removed from
    sections/product.liquid; only populated media render (4 media
    → 4 thumbs, 0 grey boxes).
[x] "Research Backed" badge links to /pages/backed-by-science
    (new optional "link" url setting on the trust_badge block;
    renders as <a> only when set; reused existing utility classes
    — no new CSS, no new JS).
[x] Mobile 390px: verified by headless-Chrome screenshot — plan
    selector stacks clean, one-time selected, badges 2+1, B/A
    leads the stack.

ALSO FIXED (audit findings, zero render impact today)
-----------------------------------------------------
- sections/product.liquid: selling-plan allocation lookup used
  `where: "selling_plan_id"` — a property that does not exist on
  selling_plan_allocation, so the computed "Save {amount} on
  refills" line, strikethrough compare price, and per-delivery
  price would NEVER render once real plans exist (card would
  silently show the one-time price). Replaced with a loop on
  alloc.selling_plan.id. Pre-existing bug, defused now.
  ⚠ RETEST the subscription card once a real selling plan is
  configured — this code path has never run against live data.
- sections/product.liquid: plan-title fallback + schema default
  were still "Refill Plan" (the label the brief calls confusing);
  both now "Kit + Monthly Refills".
- sections/benefits-grid.liquid preset default still seeded
  "…very first night." into any newly added section; default now
  carries the brief's replacement ("Partners usually notice
  before you do.").

LOGGED — NOT IMPLEMENTED (with reasons)
---------------------------------------
1) 22% DALLAM STAT CARD — NOT ADDED (brief: "log it; do not
   rebuild"). The TRAIN stat card is a {% capture %} inside
   sections/trust-bar.liquid, hard-coupled to the split layout:
   desktop = absolutely positioned over the two-column seam
   (min-height 680px grid) + parallax JS; mobile = negative-margin
   overlap on the photo column. No standalone render path;
   extracting = new section/snippet (= rebuild, forbidden).
   Exact copy for when it IS built (templates/index.json "train"):
     stat_value      "22%"
     stat_label      "More efficient breathing"
     stat_supporting "Recreational runners moved the same air
                      with up to 22% less effort after switching
                      to nasal breathing — with no drop in
                      performance."
     stat_footnote   "Dallam et al., 2018"
     footnote_url    https://journals.aiac.org.au/index.php/
                     IJKSS/article/view/4400
2) H1 LOWERCASE — no theme Liquid currently lowercases a title
   (audit note: a compiled .lowercase utility DOES exist unused
   in critical.css, so `class="lowercase"` on the H1 would work
   without any rebuild if Tom wants it — one-word change in
   sections/product.liquid). Left for Tom's call; H1 currently
   renders the admin title verbatim: "Aira Kit".
3) ADMIN STRINGS THE THEME CANNOT REACH (log per tech
   constraints):
   a. Product DESCRIPTION (admin → Products → Aira Kit). The PDP
      renders the theme subtitle override, but the admin
      description still leads with "A small…" and surfaces in
      SEO snippets/search/apps. Target copy (same as 2a):
      "A magnetic band and two skin-safe tabs that hold your nose
      open all night — quieting snoring, stopping mouth
      breathing, and deepening sleep. All four sizes in the box,
      fit guaranteed."
   b. SELLING PLAN NAME (subscription app/admin) — shown in
      cart/checkout via selling_plan.name; rename app-side if it
      should read "Kit + Monthly Refills" there too.
   c. NAVIGATION "Shop" menu item → /products/aira-kit (see
      checklist item 1).
4) SAVINGS-CLAIM COUNT — flag for Tom. Brief §2b says keep the
   label-level "Save 20% on refills" AND lists the bullet "20%
   off every refill delivery"; the checklist says "exactly
   once". Implemented literally per §2b → the selected card
   shows both (label line + bullet). If strict-once is wanted:
   blank "plan_save_text" in the theme editor (no code change).
   Note: once real plans exist the label line becomes a computed
   amount ("Save Dhs. X on refills"), not the literal "20%".
5) PRODUCT FEATURES "5-CARD SET" — the PDP grid
   (product-features-grid) is a 4-card component by design
   (block limit 4, md:grid-cols-4); it holds 4 of the homepage
   slider's 5 canonical cards (missing "Reusable Band"). A 5th
   card needs schema + grid changes (= rebuild). Brief premise
   ("already swept" 5-card) doesn't match this component.
6) REFILL HOW-TO ACCORDION — step 2 swept to canonical wording
   but without the trailing period (its siblings carry none);
   its step 3 ("Attach the magnetic band — breathe freely")
   pre-dates the canonical steps. Full canonicalization of that
   accordion belongs to the refills brief.

⚠ COHABITATION: PRE-EXISTING UNCOMMITTED SWEEP IN THIS TREE
-----------------------------------------------------------
The working tree also contains the (separate, earlier)
copy-standards sweep — see AIRA_Copy_Standards_Compliance_
Report.txt. A diff against HEAD mixes both workstreams. Items a
reviewer will otherwise misattribute to this brief, plus two
launch risks:
a. Kit FAQ q6 was rewritten by the sweep to "shipping is free
   across the GCC" — the FROZEN homepage FAQ still answers the
   same question with threshold-based free shipping. One of the
   two is false on the live site. Tom: confirm the policy, then
   either unfreeze homepage faq_6 to match or revert kit faq_6.
b. The sweep placeholdered four stats in the kit template's
   social_proof section ("XX%", "X.Xx"). The section is DISABLED
   (renders nothing — verified) and sits exactly in the slot the
   brief reserves for the 22% stat card, but ONE theme-editor
   toggle would put placeholder statistics on the page every ad
   lands on. Do not re-enable before sourcing values.
c. The sweep rewrote the refill WHY subheading to "The strip
   fixes one thing —" ("fixes" = banned verb; "strip" = wrong
   terminology) and the refill page keeps "very first night".
   This brief is scoped away from the refill page, so NOT
   changed here — but it ships in the same deploy. Ready-made
   fix if Tom extends §6 to the refill template: same two
   replacements already applied to product.json (subheading →
   "The band does one thing — keep a blocked, collapsing airway
   open." sentence; benefit_1 → "Partners usually notice before
   you do.").
d. Other sweep edits on the kit template (kept, theirs): faq_1
   "confortable"→"comfortable", faq_4 "could!"→"could.",
   shipping lead-in "Middle East"→"GCC".
e. sections/product-refill.liquid still renders empty grey
   gallery placeholder slots (the same deletion applied to
   product.liquid is available there — refills brief's call).
RECOMMENDATION: commit the compliance sweep and this kit-brief
implementation as separate commits so each can be reviewed
against its own document.

OUT OF SCOPE (unchanged, per brief)
-----------------------------------
Pricing, product photography (AEROFLOW placeholder), review app
content, full FAQ rewrite, homepage kit-contents claim (pending
SKU decision).
================================================================
