================================================================
AIRA — SITEWIDE COPY STANDARDS ENFORCEMENT
Implementation Brief for VSC (theme-wide sweep + compliance report)
Date: 2026-06-10 | Author: Tom | Status: Ready for implementation
================================================================

CONTEXT
-------
Brand & copy standards were locked 2026-06-10 (reference:
AIRA_Brand_Copy_Standards.md — context only; THIS brief is the
implementation truth). This job enforces them across the ENTIRE
theme: every template, section, snippet, and section-settings
string in templates/*.json.

EXECUTION ORDER
---------------
Run AFTER these three land (they rewrite whole blocks; sweeping
first creates merge noise):
  1. AIRA_Refills_PDP_Brief_VSC.txt
  2. AIRA_Shop_Page_Brief_VSC.txt
  3. AIRA_Homepage_Copy_Flow_Brief_VSC.txt
Where this brief and a page brief touch the same string, the
page brief wins.

HARD EXCLUSION
--------------
The homepage FAQ section is FROZEN (deferred by Tom). Zero
edits there, even for rule violations. Log its violations in
the compliance report instead (known: "confortable", shipping
contradiction, exclamation mark, casing).

----------------------------------------------------------------
SWEEP 1 — BRAND CASING
----------------------------------------------------------------
"AIRA" / "Aira" → "aira" in ALL customer-facing strings:
section settings, JSON templates, snippets, email/notification
templates if themed, meta titles/descriptions, image alt text.
- Restructure any sentence that STARTS with the brand name so
  it doesn't ("AIRA holds your..." → "aira holds your..." is
  NOT enough where it opens a sentence — rewrite the opening:
  "aira holds..." mid-sentence or "The band holds...").
  Where restructuring changes meaning, log it for Tom instead
  of improvising.
- EXCLUDE: legal entity name in footer copyright ("© 2026 aira
  Wellness." — already correct), invoice/legal docs, and the
  Shopify admin product titles (log these for Tom — admin
  content, his call).

----------------------------------------------------------------
SWEEP 2 — CTA LABELS (labels must be settings, never hardcoded)
----------------------------------------------------------------
Canonical set — these are the ONLY CTA labels on the site:
  Primary (kit):     Get my kit
  Primary (refills): Get refills
  PDP buy button:    Add to Cart
  Secondary:         How it works   |   The science
  Tutorial:          Watch tutorial
Replace ALL variants: "Shop aira kit", "Shop AIRA", "Shop Now",
"Shop the aira kit", "Learn more", "Buy Now".
If any CTA label is hardcoded in Liquid, convert to a setting
as part of this sweep.

----------------------------------------------------------------
SWEEP 3 — UNITS (tabs → pairs)
----------------------------------------------------------------
"X tabs" as a quantity/unit → "X pairs" everywhere (1 pair =
1 night; 30 pairs = 1 month).
"Adhesive tabs" as a material noun in prose stays.
Watch for: subscription app labels, cart drawer line items,
upsell blocks, meta descriptions. App-side strings you can't
reach from the theme: list them in the compliance report with
exact locations.

----------------------------------------------------------------
SWEEP 4 — SIZES
----------------------------------------------------------------
Canonical: "L1–L4" / "all four sizes, L1 to L4".
Replace: "Level 1 · Level 2 · Level 3 · Level 4", "Level 1-4",
"four bands sizes" (also a typo → "four band sizes" if the
sentence survives), any S/M/L/XL references in COPY (the
packaging artwork is out of scope).

----------------------------------------------------------------
SWEEP 5 — SHIPPING LINE
----------------------------------------------------------------
Canonical sentence: "Free shipping across the GCC."
Replace: "Free shipping on all orders", "Free shipping on all
GCC orders", "Free UAE delivery", "free shipping is available
above a certain order value" (this last one is in the frozen
FAQ — log, don't touch).
Cart drawer: "Secure checkout · Free UAE delivery" →
"Secure checkout · Free GCC delivery".

----------------------------------------------------------------
SWEEP 6 — BANNED STRINGS (grep targets; expected count after
sweep = ZERO outside the frozen FAQ)
----------------------------------------------------------------
  "Intake"                 (competitor name — critical)
  "Shop Now"
  "Doctor Recommended"
  "High Success Rate"
  "FDA registered"         (until documentation exists)
  "made in the USA"        (same)
  "Breathe Right" / "Hostage" / "Turbine"  (competitor names)
  "revolutionary" / "game-changing" / "game changer" /
  "miracle" / "literally" / "10x"
  "57%" / "75%" / "48%" / "3.5x"   (dead stats — must never
                                    resurrect anywhere)
  "designed for athletes"  → canonical origin: "Originally
                             engineered for motocross"
Exclamation marks in customer-facing copy: remove (rewrite the
sentence flat). Frozen FAQ excluded, log only.
NOTE on "game changer": it currently appears in a community
testimonial card ("Game changer for my workouts"). Testimonial
QUOTES are exempt from voice rules (they're customer speech) —
but log all placeholder testimonials in the report anyway.

----------------------------------------------------------------
SWEEP 7 — SPELLING + MECHANICS
----------------------------------------------------------------
- US English everywhere. Grep for common UK forms: -ise/-isation
  endings, "colour", "favour", "whilst", "metres". Log genuinely
  ambiguous cases rather than guessing.
- Numerals for all quantities in copy (four band sizes → 4 only
  where it's a quantity claim; "four sizes" in flowing prose is
  acceptable — when in doubt, log).
- Eyebrows: ALL CAPS, 1–3 words. Verify every section eyebrow
  complies; list violations.
- Stat cards: verify each follows number → label → finding →
  SOURCE, YEAR. Any stat WITHOUT a source in the rendered
  output = report as CRITICAL (do not delete unilaterally;
  placeholder SLEEP card rules from the 06-04 brief stand).

----------------------------------------------------------------
DELIVERABLE — COMPLIANCE REPORT
----------------------------------------------------------------
Single file: AIRA_Copy_Standards_Compliance_Report.txt
  1. Strings changed (per sweep, with file/section + before →
     after).
  2. Strings you could NOT change (app-side, admin-side, frozen
     FAQ) — exact location + suggested fix, for Tom.
  3. Judgment calls logged instead of made (per the "log, don't
     improvise" rules above).
  4. Final grep proof: every banned string at zero occurrences
     (or its only occurrences listed with reason).

TECH CONSTRAINTS
----------------
- Copy edits only. NO layout, NO design, NO new sections, NO
  Tailwind/JS changes under this brief.
- Settings/JSON edits preferred; Liquid edits only where copy
  is hardcoded (and then: convert to settings where trivial).
- Commit per sweep (7 commits) so each is reviewable/revertable.

OUT OF SCOPE
------------
- Homepage FAQ (frozen). Packaging artwork. Admin product
  titles (log only). Review app content. Meta/SEO rewrites
  beyond casing (full SEO pass is a separate upcoming brief —
  don't invent meta copy here).

ACCEPTANCE CHECKLIST
--------------------
[ ] All 7 sweeps executed in order, one commit each.
[ ] Banned-string grep returns zero hits theme-wide (frozen
    FAQ exceptions listed, nothing else).
[ ] "Intake" specifically: zero hits, confirmed in report.
[ ] Every CTA on the site renders one of the 5 canonical labels.
[ ] Cart drawer shows "Free GCC delivery" + canonical shipping
    sentence.
[ ] Compliance report delivered with all 4 sections populated.
[ ] Homepage FAQ diff = zero.
[ ] No layout/design/JS diffs anywhere (copy-only job).
================================================================