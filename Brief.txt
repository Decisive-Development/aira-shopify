================================================================
AIRA — KIT PDP FIX (/products/aira-starter-kit)
Implementation Brief for VSC (structure + copy)
Date: 2026-06-11 | Author: Tom | Status: Ready for implementation
================================================================

CONTEXT
-------
This is the page every ad dirham will land on. Audit (2026-06-11)
found: two contradictory how-to sections, the strongest proof
asset (Before/After) buried, an unreadable subscription offer,
claims-register violations, and a live "[LIST OF COUNTRIES]"
placeholder. Buy-box anatomy is good — keep the skeleton.

QUEUE POSITION: this brief runs BEFORE the homepage brief
(AIRA_Homepage_Copy_Flow_Brief_VSC.txt). Refills + Shop briefs
unaffected.

APPROVED DECISION — PLAN DEFAULT
--------------------------------
One-time Purchase stays the DEFAULT-SELECTED plan (Tom,
2026-06-11): at launch, an unknown brand converts better
without commitment friction. Do NOT pre-select the
subscription. The subscription keeps its "Best value" chip.
Revisit post-launch with data.

----------------------------------------------------------------
1. URL HANDLE + REDIRECT (decision updated 2026-06-11)
----------------------------------------------------------------
Canonical product handle: aira-kit.
- TOM does the rename in admin (Products → Aira Kit → Search
  engine listing → Edit → URL handle → "aira-kit" → tick
  "Create a URL redirect" → Save). The redirect from
  /products/aira-starter-kit is auto-created by that checkbox.
- VSC: AFTER Tom confirms the rename, sweep the theme for any
  link still targeting /products/aira-starter-kit → update to
  /products/aira-kit. (Existing briefs already target
  /products/aira-kit — no amendments needed there.)
- VSC: verify the redirect resolves (old URL → 200 at new URL).

----------------------------------------------------------------
2. BUY BOX
----------------------------------------------------------------
a) DESCRIPTION — REPLACE (current leads with "A small…" —
   leading with small is leading with an apology):
     A magnetic band and two skin-safe tabs that hold your
     nose open all night — quieting snoring, stopping mouth
     breathing, and deepening sleep. All four sizes in the
     box, fit guaranteed.

b) PLAN SELECTOR — relabel + rewrite bullets.
   Subscription option (currently "Refill Plan" — confusing
   label on a KIT page):
     label:   Kit + Monthly Refills
     chip:    Best value (keep)
     price suffix "· Save 20% on refills": keep label-level
     savings mention ONCE (no duplicate bullet).
     bullets (REPLACE all four):
       Everything in the starter kit below
       30 fresh tab pairs delivered every 30 days
       20% off every refill delivery
       Pause, skip, or cancel anytime
   One-time option:
     label:   One-time Purchase (unchanged)
     bullets  (standardize casing only):
       4 magnetic bands (L1–L4)
       15 adhesive tab pairs
       Magnetic placement tool
       Instruction manual
   DEFAULT SELECTION: One-time (see decision above).

c) CROSS-LINK: "Already have a strip? Get refills →" →
   "Already have a band? Get refills →" (terminology: band).

d) Badges row: keep as-is (Research Backed / 30-Day Guarantee /
   Medical-Grade Adhesive). "Research Backed" must link to
   /pages/backed-by-science.

----------------------------------------------------------------
3. BENEFITS ACCORDION — REPLACE ALL FIVE BULLETS
----------------------------------------------------------------
Current set has grammar drift ("Support nose breathing") and an
unsourced superlative ("Significantly reduces snoring" — claims
register violation). New set (parallel, register-compliant):
  Opens nasal passages instantly
  Quiets snoring at its source
  Keeps you nose-breathing all night
  Supports deeper, less fragmented sleep
  Reusable band — months of nights, no single-use waste

----------------------------------------------------------------
4. HOW-TO — ONE TRUTH ON THE PAGE
----------------------------------------------------------------
The page currently has TWO how-to blocks with CONTRADICTORY
placement copy ("just above the nostril" vs "either side of
your nose bridge"). Fix:
PLACEMENT TRUTH (confirmed by Tom, 2026-06-11): tabs sit JUST
ABOVE THE NOSTRILS — not on the nose bridge. The canonical
step 2 changes SITEWIDE accordingly.

CANONICAL 3 STEPS (new sitewide version):
    1. Peel the adhesive tab from its backing.
    2. Place a tab on each side of your nose, just above
       the nostril.
    3. Attach the magnetic band and breathe freely all night.

- The "HOW TO USE" accordion (buy box) adopts these 3 steps.
  Former step 4 (leaflet) becomes a small note under step 3:
    "Full instructions in the leaflet inside your kit."
- The lower "HOW IT WORKS / 3 steps to better breathing"
  SECTION IS REMOVED from this template (it duplicates the
  accordion; it lives on the homepage where it belongs).
- SITEWIDE SWEEP (part of this brief): update step 2 wherever
  the old "either side of your nose bridge" copy renders —
  known instances: homepage HOW IT WORKS section, refills PDP
  how-to accordion ("Place tabs on either side of your nose
  bridge"), and the homepage FAQ IF unfrozen later (log only
  for now — FAQ freeze stands). Grep "nose bridge" theme-wide;
  expected hits after sweep: zero outside the frozen FAQ.

----------------------------------------------------------------
5. EDUCATION STACK — REORDER + ADD PROOF
----------------------------------------------------------------
Current order below buy box:
  WHY AIRA → HOW IT WORKS (dup) → BEFORE/AFTER → PRODUCT
  features → COMMUNITY → REVIEWS → FAQ → close
TARGET order:
  BEFORE/AFTER  (promoted to directly under buy-box fold —
                 it's the demonstration; demonstration leads)
  WHY AIRA      (3 use-case blocks)
  22% STAT CARD (ADD — reuse the homepage TRAIN overlay/stat
                 component as a standalone proof strip: 22% /
                 MORE EFFICIENT BREATHING / Dallam et al., 2018
                 — exact copy from homepage card. If the
                 component can't render standalone cheaply,
                 log it; do not rebuild from scratch.)
  PRODUCT features (canonical 5-card set — already swept)
  COMMUNITY → REVIEWS → FAQ → close (unchanged order)

----------------------------------------------------------------
6. CLAIMS + COPY FIXES (WHY AIRA section, this page)
----------------------------------------------------------------
- "The strip fixes one thing — a blocked, collapsing airway."
  → "The band does one thing — keep a blocked, collapsing
     airway open." ("fixes" is banned — medical-claim verb.)
- "Most partners hear the difference on the very first night."
  → "Partners usually notice before you do." (claims register:
     anecdotal framing, no quantifier. This string survived
     the sweep — also grep the homepage WHY section for it.)
- "Snore no more" block, "aira holds/The band holds" — keep.

----------------------------------------------------------------
7. SHIPPING & RETURNS BLOCK
----------------------------------------------------------------
REPLACE the placeholder sentence:
  "We deliver to [LIST OF COUNTRIES]."
WITH:
  "We deliver to the UAE, Saudi Arabia, Kuwait, Bahrain,
   Qatar, and Oman."
Rest of the block is good — keep ("Try aira for 30 days…").

----------------------------------------------------------------
8. PAGE FAQ — MECHANICAL FIXES ONLY
----------------------------------------------------------------
The FAQ FREEZE covers the HOMEPAGE instance only. This page's
FAQ instance gets mechanical fixes (not the full parked
rewrite):
- "…this is where aira shines best The adhesive tabs…" →
  "…this is where aira shines best. The adhesive tabs…"
- "Absolutely, and honestly, this is where…" →
  "Yes — this is where…"
- "which come in packs of 30, 60, or 90 pairs" →
  "which come in packs from 30 to 360 pairs"
- Nothing else. Full FAQ rewrite remains deferred.

----------------------------------------------------------------
9. GALLERY
----------------------------------------------------------------
Same rule as refills brief: render populated media only — no
empty grey thumbnail slots. (3 of 6 currently blank.)

TECH CONSTRAINTS
----------------
- OS 2.0; settings/JSON edits preferred; copy never hardcoded.
- Subscription widget labels may live app-side — any string
  you can't reach from the theme: log location + exact target
  string in your handoff note, don't force it.
- No new Tailwind utilities, no new JS. Reordering = template
  JSON moves, not rebuilds.
- Admin product TITLE ("Aira Kit") is Tom's to change — theme
  H1 may lowercase via existing pattern if one exists; if not,
  log it.

OUT OF SCOPE
------------
- Pricing (still unset — separate admin task).
- Product photography (AEROFLOW placeholder — asset pipeline).
- Review app content/seeded reviews.
- Full FAQ rewrite (deferred — parked in
  AIRA_Copy_Flow_Audit_June2026.md §3.7).
- Homepage kit-contents claim ("30 pairs — a full month") —
  PENDING Tom's SKU decision (kit ships 15 pairs today;
  homepage Kit Offer copy will be amended once decided).

ACCEPTANCE CHECKLIST
--------------------
[ ] Handle renamed to aira-kit (Tom) + old starter-kit URL
    redirects correctly; zero theme links target the old
    handle.
[ ] "nose bridge" greps to zero theme-wide (frozen homepage
    FAQ excepted, logged).
[ ] One-time Purchase is the default-selected plan.
[ ] Subscription reads "Kit + Monthly Refills" with the four
    new bullets; savings claim appears exactly once.
[ ] Exactly ONE how-to on the page (accordion, 3 canonical
    steps + leaflet note); lower duplicate section gone.
[ ] Before/After is the first section under the buy box.
[ ] 22% Dallam stat card renders on the page (or logged).
[ ] "fixes", "Significantly reduces", "very first night",
    "[LIST OF COUNTRIES]": zero hits on this template.
[ ] Page FAQ: the three mechanical fixes applied, nothing else.
[ ] No empty gallery slots.
[ ] Copy entered exactly as written — do not improve it.
[ ] Mobile pass at 390px: plan selector, reordered stack clean.
================================================================