================================================================
AIRA — CART DRAWER POLISH
Implementation brief for VSC (copy + label logic + QA)
Date: 2026-07-25 | Author: Claude (Cowork), from Tom's cart review
Status: Ready
================================================================

CONTEXT
-------
Cart drawer reviewed 2026-07-25 from a live screenshot (Kit +
auto-added free refill in cart). FIRST: the free-gift auto-add
and Buy-X-Get-Y pricing are CONFIRMED WORKING (refill 30 line
at US$0.00 alongside the Kit) — the critical gap from
AIRA_Pricing_And_Offer_Update_Brief_VSC.txt is closed. No action
on the mechanism. This brief is presentation polish + edge-case
QA on that same drawer.

RULES: UK English, sentence case, no em-dashes, no arrows,
"aira" lowercase. Copy is final — enter exactly as written.

CHANGES (5)
-----------

1. FREE-GIFT LABEL — one instance only, on the free line
   Currently "Free 30-pack of refills" renders on BOTH the
   refill line and the Kit line. Keep it ONLY on the refill
   (US$0.00) line; remove it from the Kit line.
   IMPLEMENTATION (required, not optional): render the label
   from the line item's discount allocation (e.g. the discount
   title on line_item discount allocations), NOT as static
   copy. Then it can only appear on a line that is actually
   discounted — which also fixes edge case 5a below.

2. TRUST ROW (mid-drawer, grey, padlock icon)
   REPLACE: "Secure checkout · Free UAE delivery"
   WITH:    "Secure checkout"
   (The shipping claim moves to one place — see 4. The UAE-only
   wording was also stale: we ship UAE + KSA.)

3. BOILERPLATE LINE (under Subtotal)
   REPLACE: "Shipping & taxes calculated at checkout"
   WITH:    "Taxes calculated at checkout"
   (Shipping is free and stated on the orange line directly
   below; "shipping calculated at checkout" contradicts it.
   Keep the taxes wording generic — the VAT decision is still
   open, this line is safe either way.)

4. FREE-SHIPPING LINE (orange, under Subtotal)
   REPLACE: "Free shipping in the UAE."
   WITH:    "Free shipping across the UAE and Saudi Arabia."
   NOTE: same bug family as the homepage meta (UAE-only copy
   predating the Saudi market). While in there, grep the cart/
   checkout-adjacent snippets for any other "in the UAE"
   shipping strings and apply the same fix; list what you
   change.

5. DRAWER TITLE
   REPLACE: "Your Cart"
   WITH:    "Your cart"
   (Sentence case sitewide standard.)

QA CHECKLIST (edge cases on the auto-add + BXGY — test all)
-----------------------------------------------------------
[ ] 5a. Remove the KIT, leave the refill: Shopify reprices the
    refill to $19.99. The free-gift label must NOT render on a
    $19.99 line (guaranteed by the allocation-driven label in
    change 1).
[ ] 5b. Bump the free refill line's quantity to 2: exactly one
    unit stays $0.00, the second charges $19.99 (BXGY allocates
    per the discount config). Confirm the line presentation
    doesn't imply both are free. If the theme can, LOCK the
    quantity stepper on the $0.00 gift line (qty fixed at 1);
    a paying customer who wants more refills should add the
    refill product normally.
[ ] 5c. Delete the free refill line as a customer: the auto-add
    must NOT re-add it in an endless loop. Rule: re-add only
    when the Kit is (re)added or its quantity increases, never
    on generic cart changes after a manual removal.
[ ] 5d. Two Kits in cart: confirm expected behaviour (discount
    is buy 1 get 1: does a second Kit earn a second free pouch?
    Match the cart to whatever the discount actually does).
[ ] Standard pass: drawer renders correctly with only refills
    (no Kit), and with an empty cart.

OUT OF SCOPE (tracked elsewhere)
--------------------------------
- Refill product imagery (blank thumbnail): photography batch.
- Product display titles "Aira Kit" / "Aira refill tabs" →
  lowercase "aira kit" / "aira refill tabs": CATALOG edit
  (admin, not theme) — Tom to approve; ripples into checkout
  and notification emails, which is desirable but his call.
- Variant label rendering "30" bare (could read "30 pairs"):
  option-value naming, decide once with Tom (ripples to PDP
  selector chips).
- Checkout page itself (Shopify-hosted).

ACCEPTANCE
----------
[ ] One free-gift label total, on the $0.00 line only, driven
    by discount allocations.
[ ] Drawer shows exactly one shipping message: the orange
    UAE + Saudi line.
[ ] All five copy strings exactly as written above.
[ ] All QA boxes ticked, with a one-line note per edge case on
    observed behaviour.
================================================================