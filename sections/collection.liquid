{%- liquid
	case section.settings.padding_vertical
		when 'none'
			assign pt = ''
			assign pb = ''
		when 'sm'
			assign pt = 'pt-8 md:pt-12'
			assign pb = 'pb-8 md:pb-12'
		when 'md'
			assign pt = 'pt-16 md:pt-20'
			assign pb = 'pb-16 md:pb-20'
		when 'lg'
			assign pt = 'pt-24 md:pt-32'
			assign pb = 'pb-24 md:pb-32'
	endcase

	case section.settings.columns
		when '2'
			assign grid_cols = 'grid-cols-1 sm:grid-cols-2'
		when '3'
			assign grid_cols = 'grid-cols-1 sm:grid-cols-2 lg:grid-cols-3'
		when '4'
			assign grid_cols = 'grid-cols-2 sm:grid-cols-2 lg:grid-cols-4'
	endcase
-%}

{%- comment -%} Page header — dark bg so transparent nav is visible {%- endcomment -%}
<div class="bg-main pt-32 pb-16 md:pt-36 md:pb-20">
	<div class="container-contained text-center">
		{%- if section.settings.eyebrow != blank -%}
			<p class="text-xs font-bold uppercase tracking-widest text-highlight mb-4">{{ section.settings.eyebrow }}</p>
		{%- endif -%}
		<h1 class="font-heading font-bold text-4xl md:text-5xl leading-tight tracking-tight text-secondary">
			{{ collection.title }}
		</h1>
		{%- if collection.description != blank -%}
			<p class="text-base md:text-lg leading-relaxed text-secondary opacity-65 max-w-2xl mx-auto mt-4">{{ collection.description }}</p>
		{%- endif -%}
	</div>
</div>

<section style="background-color: {{ section.settings.background_color }};">
	<div class="container-contained {{ pt }} {{ pb }}">

		{%- comment -%} Sort & filter bar {%- endcomment -%}
		<div class="flex items-center justify-between mb-8 pb-4 border-b border-bg-muted">
			<p class="text-sm text-copy">
				{{ collection.products_count }} {{ collection.products_count | pluralize: 'product', 'products' }}
			</p>

			<div class="flex items-center gap-2">
				<label for="sort-select" class="text-sm font-semibold text-title hidden sm:block">Sort by</label>
				<div class="relative">
					<select
						id="sort-select"
						class="appearance-none bg-bg-soft border border-bg-muted rounded-xl text-sm font-semibold text-title pl-4 pr-9 py-2.5 cursor-pointer focus:outline-none focus:border-highlight-blue transition-colors"
						data-sort-select
					>
						{%- for option in collection.sort_options -%}
							<option
								value="{{ option.value }}"
								{% if collection.sort_by == option.value or collection.default_sort_by == option.value and collection.sort_by == blank %}selected{% endif %}
							>
								{{ option.name }}
							</option>
						{%- endfor -%}
					</select>
					<svg class="absolute right-3 top-1/2 -translate-y-1/2 pointer-events-none text-copy" xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
						<path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/>
					</svg>
				</div>
			</div>
		</div>

		{%- comment -%} Product grid {%- endcomment -%}
		{%- paginate collection.products by section.settings.products_per_page -%}
			{%- if collection.products.size > 0 -%}
				<div class="grid {{ grid_cols }} gap-6 lg:gap-8">
					{%- for product in collection.products -%}
						{%- render 'product-card', product: product -%}
					{%- endfor -%}
				</div>
			{%- else -%}
				<div class="text-center py-20">
					<p class="text-copy text-lg">No products found in this collection.</p>
				</div>
			{%- endif -%}

			{%- comment -%} Pagination {%- endcomment -%}
			{%- if paginate.pages > 1 -%}
				<nav class="flex items-center justify-center gap-2 mt-14" aria-label="Collection pagination">
					{%- if paginate.previous -%}
						<a
							href="{{ paginate.previous.url }}"
							class="w-10 h-10 flex items-center justify-center rounded-xl text-copy transition-colors hover:bg-bg-soft"
							aria-label="Previous page"
						>
							<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
								<path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/>
							</svg>
						</a>
					{%- endif -%}

					{%- for part in paginate.parts -%}
						{%- if part.is_link -%}
							<a
								href="{{ part.url }}"
								class="w-10 h-10 flex items-center justify-center rounded-xl text-sm font-semibold text-copy transition-colors hover:bg-bg-soft"
							>
								{{ part.title }}
							</a>
						{%- else -%}
							{%- if part.title == paginate.current_page -%}
								<span class="w-10 h-10 flex items-center justify-center rounded-xl text-sm font-bold bg-main text-secondary">
									{{ part.title }}
								</span>
							{%- else -%}
								<span class="w-10 h-10 flex items-center justify-center text-sm text-copy opacity-50">
									{{ part.title }}
								</span>
							{%- endif -%}
						{%- endif -%}
					{%- endfor -%}

					{%- if paginate.next -%}
						<a
							href="{{ paginate.next.url }}"
							class="w-10 h-10 flex items-center justify-center rounded-xl text-copy transition-colors hover:bg-bg-soft"
							aria-label="Next page"
						>
							<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
								<path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
							</svg>
						</a>
					{%- endif -%}
				</nav>
			{%- endif -%}
		{%- endpaginate -%}

	</div>
</section>

<script>
(function () {
	/* Sort select → navigate */
	var sortSelect = document.querySelector('[data-sort-select]');
	if (sortSelect) {
		sortSelect.addEventListener('change', function () {
			var url = new URL(window.location.href);
			url.searchParams.set('sort_by', this.value);
			window.location.href = url.toString();
		});
	}

	/* Product card variant JS (same as featured-products) */
	document.querySelectorAll('[data-product-card]').forEach(function (card) {
		var variantInput = card.querySelector('[data-variant-id]');
		var priceEl      = card.querySelector('[data-price]');
		var addBtn       = card.querySelector('[data-add-to-cart]');
		var imgEl        = card.querySelector('a img');
		var dataEl       = card.querySelector('[data-product-variants]');
		if (!dataEl) return;
		var variants     = JSON.parse(dataEl.textContent);
		var selectedOptions = variants[0].options.slice();

		function findVariant() {
			return variants.find(function (v) {
				return v.options.every(function (opt, i) {
					return opt === selectedOptions[i];
				});
			}) || null;
		}

		function updateCard(variant) {
			if (!variant) return;
			variantInput.value  = variant.id;
			priceEl.textContent = variant.price_formatted;
			addBtn.disabled     = !variant.available;
			addBtn.textContent  = variant.available ? 'Add to Cart' : 'Sold Out';
			if (variant.image && imgEl) {
				imgEl.removeAttribute('srcset');
				imgEl.removeAttribute('sizes');
				imgEl.src = variant.image;
			}
		}

		card.querySelectorAll('[data-option-btn]').forEach(function (btn) {
			btn.addEventListener('click', function () {
				var idx      = parseInt(this.dataset.optionIndex);
				var value    = this.dataset.optionValue;
				selectedOptions[idx] = value;

				var group    = card.querySelector('[data-option-group="' + idx + '"]');
				var isColour = group.dataset.type === 'colour';

				group.querySelectorAll('[data-option-btn]').forEach(function (b) {
					var active = b.dataset.optionValue === value;
					if (isColour) {
						if (active) {
							b.classList.add('border-main', 'ring-2', 'ring-main', 'ring-offset-1');
							b.classList.remove('border-transparent');
						} else {
							b.classList.remove('border-main', 'ring-2', 'ring-main', 'ring-offset-1');
							b.classList.add('border-transparent');
						}
					} else {
						if (active) {
							b.classList.add('bg-main', 'text-secondary', 'border-main');
							b.classList.remove('bg-bg', 'border-bg-muted', 'text-copy');
						} else {
							b.classList.remove('bg-main', 'text-secondary', 'border-main');
							b.classList.add('bg-bg', 'border-bg-muted', 'text-copy');
						}
					}
				});

				updateCard(findVariant());
			});
		});
	});
})();
</script>

{% schema %}
{
	"name": "Collection",
	"settings": [
		{
			"type": "header",
			"content": "Heading"
		},
		{
			"type": "text",
			"id": "eyebrow",
			"label": "Eyebrow"
		},
		{
			"type": "header",
			"content": "Grid"
		},
		{
			"type": "select",
			"id": "columns",
			"label": "Columns",
			"options": [
				{ "value": "2", "label": "2 columns" },
				{ "value": "3", "label": "3 columns" },
				{ "value": "4", "label": "4 columns" }
			],
			"default": "3"
		},
		{
			"type": "range",
			"id": "products_per_page",
			"label": "Products per page",
			"min": 4,
			"max": 24,
			"step": 2,
			"default": 12
		},
		{
			"type": "header",
			"content": "Background"
		},
		{
			"type": "color",
			"id": "background_color",
			"label": "Background color",
			"default": "#FFFFFF"
		},
		{
			"type": "header",
			"content": "Layout"
		},
		{
			"type": "select",
			"id": "padding_vertical",
			"label": "Vertical padding",
			"options": [
				{ "value": "none", "label": "None" },
				{ "value": "sm",   "label": "Small" },
				{ "value": "md",   "label": "Medium" },
				{ "value": "lg",   "label": "Large" }
			],
			"default": "lg"
		}
	]
}
{% endschema %}
