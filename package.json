{
  "name": "aira-shopify",
  "version": "1.0.0",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./assets/critical.source.css -o ./assets/critical.css",
    "watch:css": "npx @tailwindcss/cli -i ./assets/critical.source.css -o ./assets/critical.css --watch",
    "dev": "shopify theme dev --theme Aira-theme --theme-editor-sync -s aira-breathing",
    "dev:css": "npm run watch:css"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "tailwindcss": "^4.1.17"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
